import styles from '../../styles/Home.module.css'

const Navbar = () => {
    return ( 
        <nav className={styles.content}>
            <code className={styles.code}>AdazolHUB</code>
            <code className={styles.code}>Next JS</code>
        </nav>
     );
}
 
export default Navbar;
